import 'fraction.dart';
void main() {
  var a = Fraction(3, 6), b = Fraction(4, 2);
  (a + b).display();
  (a - b).display();
  (a * b).display();
  (a / b).display();
  (-a).display();
  (-b).display();
}